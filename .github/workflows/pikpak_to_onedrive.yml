name: Transfer Files from PikPak to OneDrive

on:
  workflow_dispatch:
    inputs:
      path:
        description: 'The path to copy'
        required: true
        default: 'example/path'

jobs:
  transfer:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Rclone
      run: |
        curl https://rclone.org/install.sh | sudo bash
        mkdir -p ~/.config/rclone
        echo "${{ secrets.RCLONE_CONF }}" > ~/.config/rclone/rclone.conf

    - name: Verify Rclone Configuration
      run: cat ~/.config/rclone/rclone.conf

    - name: List Configured Remotes
      run: rclone listremotes

    - name: Check Rclone Version
      run: rclone version

    - name: Transfer files from PikPak to OneDrive
      run: |
        rclone copy p:${{ github.event.inputs.path }} o:${{ github.event.inputs.path }} --verbose --log-file=rclone.log
        cat rclone.log

    - name: Delete workflow runs
      uses: Mattraks/delete-workflow-runs@v1.2.3
      with:
        token: ${{ github.token }}
        repository: ${{ github.repository }}
        retain_days: 0
        keep_minimum_runs: 0
